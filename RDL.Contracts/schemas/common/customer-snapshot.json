{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.rdl.invalid/schemas/common/customer-snapshot.json",
  "title": "CustomerSnapshot",
  "description": "Datos del cliente copiados al emitir (arquitectura 4.4). Mismos campos que billing.invoices.customer_*: un cambio posterior del cliente no los altera.",
  "type": "object",
  "additionalProperties": false,
  "required": ["customerId", "identification", "legalName"],
  "properties": {
    "customerId": { "$ref": "uuid.json" },
    "identification": { "$ref": "identification.json" },
    "legalName": { "type": "string", "minLength": 1, "maxLength": 200 },
    "email": { "type": "string", "format": "email", "maxLength": 254 },
    "phone": { "type": "string", "maxLength": 30 },
    "address": { "type": "string", "maxLength": 500, "description": "Dirección en texto libre. TODO(fiscal): códigos de provincia, cantón y distrito si los exige el comprobante." }
  }
}
